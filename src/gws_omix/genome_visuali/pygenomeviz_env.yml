name: pygenomeviz
channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  - pygenomeviz==1.6.1
  - biopython               # Required for FASTA extraction (SeqIO)
  - mummer                  # Required for MUMmer genome comparison
  - blast                   # Required for BLAST genome comparison
